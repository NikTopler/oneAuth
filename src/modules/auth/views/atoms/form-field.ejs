<div id="form-<%=name%>-field-container">
  <label for="<%=name%>-field"><%=label%></label>
  <input
    id="<%=name%>-field"
    type="<%=type%>"
    name="<%=name%>-field"
    value="<%=value%>"

    <% if(pattern) { %> pattern="<%=pattern%>" <% } %>
    <% if(autocomplete) { %> autocomplete="<%=autocomplete%>" <% } %>

    <% if(serverValidation !== false) { %>  
      hx-post="<%=serverValidation.validationUrl%>"
      hx-indicator="#<%=name%>-validator"
      hx-target="#form-<%=name%>-field-container"
      hx-swap="outerHTML" 
    <% } %>
  
    required="<%=required%>"
  />

  <span id="<%=name%>-validator" class="form-validator">Validating...</span>

  <% if(!valid && value.length > 0) { %>
  <span style="color: red"> <%= errorMessage %> </span>
  <% } %>
</div>
